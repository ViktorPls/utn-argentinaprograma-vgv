// Ejercicio 4 
function cuadrado(lado) {
    var perimetro = (lado * 4)
    var superficie = (lado * lado)
    console.log("El perimetro del cuadrado es " + perimetro + " y la superficie es " + superficie)
}
cuadrado(4);